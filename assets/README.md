# Assets

Place `stratarius-logo.png` here. The sidebar reads it at runtime and embeds it as a data-URL, so no static-serving config is needed.

If the file is missing, the sidebar falls back to a text-only header.
